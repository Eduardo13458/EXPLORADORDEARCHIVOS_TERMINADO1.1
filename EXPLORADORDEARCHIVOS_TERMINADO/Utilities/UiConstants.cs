namespace EXPLORADORDEARCHIVOS_TERMINADO.Utilities;

public static class UiConstants
{
    // Número máximo de filas a mostrar en previsualizaciones y listados cortos
    public const int MAX_ROWS_DISPLAYED_IN_PREVIEW = 15;

    // Número máximo de duplicados a mostrar en listados (si aplica)
    public const int MAX_DUPLICATES_SHOWN = 20;

    // Percentil por defecto usado para filtros dinámicos (p. ej. 0.75 = P75)
    public const double DEFAULT_PERCENTILE_THRESHOLD = 0.75;
}
